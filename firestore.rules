rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The authenticated Node API uses the Admin SDK and IAM.
    // Browsers do not access Firestore directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
